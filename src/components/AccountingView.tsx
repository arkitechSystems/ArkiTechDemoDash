import React from 'react';

const AccountingView: React.FC = () => {
  return (
    <main className="dashboard" role="main" aria-label="Accounting View">
      {/* Blank page for now - will add accounting content later */}
    </main>
  );
};

export default AccountingView;

========================================
RENDER DEPLOYMENT GUIDE - CchdDash
========================================

PROJECT OVERVIEW:
This is a full-stack healthcare financial dashboard application with:
- Frontend: React (Create React App with TypeScript)
- Backend: Node.js + Express + TypeScript
- Database: SQLite (embedded)
- Authentication: JWT tokens with bcrypt password hashing + MFA support
- Security: Audit logging, role-based access, password policies

TARGET DEPLOYMENT:
- Frontend: https://connect301.arkitech-test.xyz/
- Backend API: https://api.connect301.arkitech-test.xyz/

========================================
RECOMMENDED DEPLOYMENT: BLUEPRINT (EASIEST)
========================================

Your project includes a render.yaml file that automates deployment of both services.

STEPS:
------
1. Push your latest code to GitHub:
   git add .
   git commit -m "Prepare for deployment"
   git push origin master

2. Log in to Render (https://render.com)

3. Click "New +" → "Blueprint"

4. Connect your GitHub repository:
   - Repository: https://github.com/Wmartin23/CchdDash.git
   - Branch: master

5. Render will automatically detect render.yaml and show you:
   - Backend API Service (cchddash-api)
   - Frontend Static Site (cchddash-frontend)

6. Review the configuration and click "Apply"

7. Wait for both services to build and deploy (5-10 minutes)

8. Your services will initially be live at:
   - Backend API: https://cchddash-api.onrender.com
   - Frontend: https://cchddash-frontend.onrender.com

IMPORTANT NOTES:
- Both services are FREE on the free tier
- Backend will auto-generate a secure JWT_SECRET
- Database is SQLite (ephemeral on free tier - resets on restart)
- Auto-deploy when you push to GitHub
- Default user: Wmartin (ask admin for password)

========================================
CUSTOM DOMAIN SETUP (connect301.arkitech-test.xyz)
========================================

STEP 1: DNS CONFIGURATION
--------------------------
At your domain registrar (where arkitech-test.xyz is managed), add these CNAME records:

For Frontend:
  Type:  CNAME
  Name:  connect301
  Value: cchddash-frontend.onrender.com
  TTL:   3600 (or Automatic)

For Backend API:
  Type:  CNAME
  Name:  api.connect301
  Value: cchddash-api.onrender.com
  TTL:   3600 (or Automatic)

STEP 2: ADD CUSTOM DOMAINS IN RENDER
-------------------------------------
Frontend Custom Domain:
1. In Render Dashboard, go to "cchddash-frontend" service
2. Navigate to "Settings" → "Custom Domain"
3. Click "Add Custom Domain"
4. Enter: connect301.arkitech-test.xyz
5. Click "Save"
6. Wait for SSL certificate (Let's Encrypt) - usually 1-5 minutes

Backend API Custom Domain:
1. In Render Dashboard, go to "cchddash-api" service
2. Navigate to "Settings" → "Custom Domain"
3. Click "Add Custom Domain"
4. Enter: api.connect301.arkitech-test.xyz
5. Click "Save"
6. Wait for SSL certificate - usually 1-5 minutes

STEP 3: VERIFY DEPLOYMENT
--------------------------
Test Backend API:
  curl https://api.connect301.arkitech-test.xyz/api/health

  Expected response:
  {"status":"ok","message":"Server is running"}

Test Frontend:
  Open browser: https://connect301.arkitech-test.xyz/
  You should see the login page with ArkiTech branding

STEP 4: VERIFY ENVIRONMENT VARIABLES
-------------------------------------
Frontend (cchddash-frontend):
  REACT_APP_API_URL=https://api.connect301.arkitech-test.xyz

Backend (cchddash-api):
  NODE_ENV=production
  JWT_SECRET=<auto-generated-or-set-your-own>
  PORT=3001

These should already be set from render.yaml, but verify in Render Dashboard.

========================================
OPTION 2: MANUAL DEPLOYMENT (TWO SEPARATE SERVICES)
========================================

If you prefer to deploy the frontend and backend separately:

BACKEND API (Web Service):
--------------------------
1. Go to Render Dashboard
2. Click "New +" → "Web Service"
3. Connect your GitHub repository
4. Configure:
   - Name: cchddash-api
   - Environment: Node
   - Build Command: cd server && npm install && npm run build
   - Start Command: cd server && npm start
   - Instance Type: Free

5. Add Environment Variables:
   - NODE_ENV=production
   - JWT_SECRET=<generate-a-random-secure-string-32-chars>
   - PORT=3001

6. Click "Create Web Service"
7. After deployment, add custom domain: api.connect301.arkitech-test.xyz

FRONTEND (Static Site):
-----------------------
1. Go to Render Dashboard
2. Click "New +" → "Static Site"
3. Connect your GitHub repository
4. Configure:
   - Name: cchddash-frontend
   - Build Command: npm install && npm run build
   - Publish Directory: build
   - Auto-Deploy: Yes

5. Add Environment Variables:
   - REACT_APP_API_URL=https://api.connect301.arkitech-test.xyz

6. Click "Create Static Site"
7. After deployment, add custom domain: connect301.arkitech-test.xyz

========================================
USER MANAGEMENT
========================================

DEFAULT USER:
-------------
- Username: Wmartin
- Password: (set by administrator)
- Role: admin (full access)

CREATE NEW USERS:
-----------------
Method 1 - Via Command Line Script (Recommended):
  cd server
  npm run create-user <username> <password> <email> <role>

  Example:
  npm run create-user jsmith SecurePass123! john@arkitech.com dashboard

  Available roles: dashboard, accountant, both, admin

Method 2 - Via API:
  curl -X POST https://api.connect301.arkitech-test.xyz/api/auth/register \
    -H "Content-Type: application/json" \
    -d '{"username":"newuser","password":"SecurePass123!","email":"user@example.com","role":"dashboard"}'

LIST ALL USERS:
---------------
  cd server
  npm run list-all-users

RESET USER PASSWORD:
--------------------
  cd server
  npm run reset-password <username> <new-password>

  Example:
  npm run reset-password jsmith NewPassword123!

DELETE USER:
------------
  cd server
  npm run delete-user <username>

VIEW AUDIT LOGS:
----------------
  cd server
  npm run view-logs

For complete user management guide, see USER_MANAGEMENT.md

========================================
SECURITY FEATURES
========================================

✓ JWT Authentication with secure tokens
✓ bcrypt password hashing (10 salt rounds)
✓ Multi-Factor Authentication (MFA/2FA) support
✓ Role-Based Access Control (RBAC)
✓ Password complexity requirements
✓ First-login password reset enforcement
✓ Comprehensive audit logging
✓ Session management with inactivity timeout
✓ CORS protection (whitelisted domains only)
✓ HTTPS enforcement in production
✓ Secure HTTP headers
✓ SQL injection protection

For security documentation, see SECURITY.md

========================================
TROUBLESHOOTING
========================================

DNS NOT RESOLVING:
------------------
Issue: Domain doesn't resolve to Render
Solutions:
  - DNS changes can take 5 minutes to 48 hours
  - Use: nslookup connect301.arkitech-test.xyz
  - Verify CNAME records are correct
  - Check DNS propagation: https://dnschecker.org

CORS ERRORS:
------------
Issue: Frontend can't connect to backend
Solutions:
  - Verify REACT_APP_API_URL is set correctly
  - Check allowed origins in server/src/server.ts
  - Ensure custom domain is in allowedOrigins array
  - Clear browser cache and hard reload

SSL CERTIFICATE ISSUES:
-----------------------
Issue: "Not Secure" warning in browser
Solutions:
  - Wait 5-10 minutes for Let's Encrypt provisioning
  - Verify DNS is pointing to Render correctly
  - Check Render Dashboard for SSL status
  - Try accessing via HTTPS explicitly

BUILD FAILURES:
---------------
- Check build logs in Render Dashboard
- Verify all dependencies are in package.json
- Ensure Node version compatibility (16+)
- Check for TypeScript errors

ROUTING ISSUES (404 on page refresh):
--------------------------------------
- Frontend routing is handled by the rewrite rule in render.yaml
- This should already be configured, but verify:
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

DATABASE RESETS:
----------------
Issue: Users/data disappear after service restart
Cause: SQLite is ephemeral on Render free tier
Solutions:
  - Upgrade to Render paid plan for persistent disk
  - Migrate to PostgreSQL (recommended for production)
  - Accept that free tier resets on restart

SCREENSAVER ISSUES:
-------------------
- Screensaver appears after 1 minute of inactivity
- Auto-logout after 10 minutes of inactivity
- Click anywhere to dismiss screensaver
- To adjust timing, edit: src/contexts/AuthContext.tsx

========================================
CURRENT PROJECT STATUS
========================================

Your application includes:
✓ Full-stack React + Node.js architecture
✓ Professional login page with glassmorphic design
✓ JWT authentication with bcrypt password hashing
✓ Multi-Factor Authentication (MFA) support with TOTP
✓ Role-based access control (4 roles)
✓ First-login password reset flow
✓ Comprehensive audit logging system
✓ Session management with screensaver
✓ SQLite database for user/session storage
✓ Backend API with Express + TypeScript
✓ Frontend connected to backend API
✓ Email notification support (SMTP configurable)
✓ Inactivity timeout with auto-logout
✓ Custom domain configuration
✓ HTTPS/SSL ready
✓ CORS security configured
✓ Ready for production deployment

========================================
QUICK START COMMANDS
========================================

LOCAL DEVELOPMENT:
------------------
1. Start backend (in one terminal):
   cd server
   npm run dev
   (Runs on http://localhost:3001)

2. Start frontend (in another terminal):
   npm start
   (Runs on http://localhost:3000)

3. Login with your configured user credentials

PRODUCTION DEPLOYMENT:
----------------------
1. Commit and push to GitHub:
   git add .
   git commit -m "Deploy to production"
   git push origin master

2. Render will auto-deploy both services

3. Configure DNS and custom domains (one-time setup)

4. Access at: https://connect301.arkitech-test.xyz/

USER MANAGEMENT:
----------------
Create user:
  cd server && npm run create-user <user> <pass> <email> <role>

List users:
  cd server && npm run list-all-users

Reset password:
  cd server && npm run reset-password <user> <new-pass>

View audit logs:
  cd server && npm run view-logs

========================================
IMPORTANT FILES
========================================

Configuration:
- render.yaml              - Render deployment configuration
- .env.example             - Frontend environment template
- server/.env.example      - Backend environment template

Documentation:
- DEPLOYMENT.md            - Full deployment guide
- CUSTOM_DOMAIN_SETUP.md   - Custom domain setup guide
- USER_MANAGEMENT.md       - User administration guide
- SECURITY.md              - Security features documentation
- MFA_GUIDE.md             - Multi-factor auth setup
- AUDIT_LOGGING.md         - Audit system documentation

Database:
- server/database.sqlite   - User database (auto-created)
- server/auditlog.txt      - Audit log file

Scripts:
- server/src/resetPassword.ts      - Password reset utility
- server/src/listAllUsers.ts       - User listing utility
- server/src/deleteUser.ts         - User deletion utility
- server/src/viewAuditLogs.ts      - Audit log viewer

========================================
RECOMMENDED NEXT STEPS
========================================

FOR IMMEDIATE DEPLOYMENT:
1. Push code to GitHub
2. Deploy via Render Blueprint
3. Configure DNS records
4. Add custom domains in Render
5. Test the deployment
6. Create additional user accounts

FOR PRODUCTION READINESS:
1. Configure email SMTP settings (see EMAIL_SETUP.md)
2. Enable MFA for admin users
3. Review audit logs regularly
4. Set up monitoring/alerting
5. Consider upgrading to Render paid plan
6. Migrate to PostgreSQL if needed
7. Set up automated backups
8. Configure rate limiting
9. Review security settings

========================================
SUPPORT & RESOURCES
========================================

Documentation:
- Render Docs: https://render.com/docs
- Blueprint Guide: https://render.com/docs/infrastructure-as-code
- Custom Domains: https://render.com/docs/custom-domains

Project Resources:
- GitHub Repo: https://github.com/Wmartin23/CchdDash
- Deployment Docs: See DEPLOYMENT.md and CUSTOM_DOMAIN_SETUP.md
- Security Docs: See SECURITY.md

Support:
- ArkiTech Systems Support
- Render Support: support@render.com (for platform issues)

========================================

DEPLOYMENT TARGET:
Production URL: https://connect301.arkitech-test.xyz/
API Endpoint:   https://api.connect301.arkitech-test.xyz/

LAST UPDATED: 2025-10-21
Current Version: Full-stack with JWT auth, MFA, RBAC, audit logging,
                 screensaver, and custom domain configuration

========================================
